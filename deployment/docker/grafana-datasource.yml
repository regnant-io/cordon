apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Prometheus shares the cordon service's network namespace, so it is
    # addressed by that service's name rather than its own.
    url: http://cordon:9090
    isDefault: true
    editable: false
